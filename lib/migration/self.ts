/**
 * The refusal a Deplo instance pasted as the source gets. Shared, because the
 * wizard hides its "Which one is this?" picker on exactly this one - neither
 * answer is it.
 */
export const SELF_PANEL_REFUSAL =
  "That address is a Deplo instance. Moving between two Deplo instances is not supported yet.";
